# DocSwift 发布自动化设计思路

## 1. 目标

发布自动化的核心不是“少敲几条命令”，而是把一次发布变成一个可验证、可重试、
不容易误操作的事务。无论后续使用 PowerShell、Python 还是独立桌面发布工具，
都应保持下面这条主线：

> 先验证本地成果，再保存源码版本，然后发布下载包，最后才让客户端看见新版本。

这样可以避免客户端读到一个尚未上传完成、校验不通过或无法回滚的版本。

## 2. 发布对象分为三层

### 2.1 源码版本

公司 Gitea 保存项目源码、提交记录和 Git 标签。它回答：

- 这个版本由哪一次代码提交构建？
- 当时修改了什么？
- 出现问题时应该回到哪个版本？

版本标签必须固定指向一个提交，已经发布的标签不得静默移动。

### 2.2 人工下载的发布版

Gitea 发布版保存 Windows 免安装 ZIP、校验文件和发布说明。它面向首次安装、
人工重装和历史版本下载。

发布版应包含：

- 清晰的版本号；
- 对应 Git 标签；
- Windows 免安装 ZIP；
- SHA-256 校验信息；
- 面向使用者的更新说明。

### 2.3 客户端自动更新源

Ubuntu 上的 Nginx 更新目录面向程序内“检查更新”。客户端先读取 `latest.json`，
比较版本后再下载指定 ZIP，并核对文件大小和 SHA-256。

`latest.json` 是更新系统的“总开关”。必须在 ZIP 已上传且校验成功之后最后更新，
否则客户端可能发现一个尚不可用的新版本。

## 3. 推荐发布流水线

一次完整发布应按固定顺序执行：

1. 校验版本号、当前分支、工作区和发布配置。
2. 更新程序内部版本号及用户文档。
3. 运行语法检查、单元测试和关键回归测试。
4. 构建 Windows 免安装 ZIP。
5. 计算 ZIP 的 SHA-256 和准确字节数。
6. 生成校验文件与内部更新清单。
7. 提交源码并创建不可变 Git 标签。
8. 推送到公司 Gitea。
9. 创建 Gitea 发布版并上传附件。
10. 将 ZIP 上传到 Ubuntu 临时位置。
11. 在 Ubuntu 再次核对 SHA-256。
12. 将 ZIP 放入正式版本目录。
13. 最后原子替换公开的 `latest.json`。
14. 从员工实际访问地址重新读取清单并检查下载文件。

前一步失败时不得继续执行依赖它的后一步。

## 4. 安全和可靠性原则

### 4.1 凭据不进入项目

Gitea API 令牌、SSH 私钥和密码不得写进源码、配置模板、日志或发布说明。
桌面发布工具应从 Windows 凭据管理器、受保护的系统环境变量或安全配置中读取。

### 4.2 默认人工确认

正式发布前应明确显示：

- 应用名称和版本；
- 当前分支及目标分支；
- 将要提交的文件；
- Gitea 仓库；
- Ubuntu 更新目录；
- 发布包名称、大小和 SHA-256。

操作者确认后才允许发生提交、推送、创建发布版和服务器上传。

### 4.3 可重试但不破坏旧版本

网络中断后再次执行同一版本时：

- 正确的已有提交和标签应复用；
- 标签指向其他提交时必须停止；
- 同名且一致的发布附件可以跳过；
- 同名但内容不同的附件必须明确替换或拒绝；
- 旧版本目录和旧发布版不得删除。

### 4.4 先上传文件，最后公开清单

自动更新必须采用“两阶段发布”：

1. 上传和验证新版本文件；
2. 更新 `latest.json` 使新版本对客户端可见。

如果第二阶段之前失败，现有客户端仍会继续使用旧版本，不会受到半成品影响。

### 4.5 日志应可审计但不得泄密

每次发布应保存开始时间、结束时间、操作者、版本、提交、标签、文件哈希、
各步骤结果和错误摘要。日志不得记录 API 令牌、密码、私钥或完整认证请求头。

## 5. 独立桌面发布工具建议

后续桌面工具可以把发布流程设计成一个明确的状态机：

`准备 → 检查 → 测试 → 构建 → 提交 → Gitea 发布 → 服务器发布 → 外部验证 → 完成`

建议界面包括：

- 项目配置列表，可支持 DocSwift、DrawingPdfRenamer 和未来项目；
- 版本号、发布说明和目标渠道输入；
- 待提交文件、测试结果和包信息预览；
- 分步骤进度、实时日志和失败位置；
- “仅检查”“本地构建”“正式发布”三种模式；
- 失败后从安全步骤重试；
- 发布历史及每个版本的 SHA-256；
- 紧急停止功能，但停止时不得破坏已发布的旧版本；
- 发布完成后的 Gitea 页面、清单地址和下载地址快捷入口。

项目差异应放在配置和适配器中，通用发布引擎不直接写死某一个项目：

- 构建命令；
- 测试命令；
- 应用版本文件；
- 包名规则；
- Gitea 仓库；
- Ubuntu 更新目录；
- 内部清单地址。

## 6. 发布工具自身的边界

发布工具可以自动执行机械步骤，但不应替代发布决策。以下情况应立即停止并要求
人工处理：

- 测试失败；
- 工作区包含无法解释的文件；
- 版本标签冲突；
- 构建包哈希不一致；
- Gitea 或 Ubuntu 身份验证失败；
- 服务器空间不足；
- 对外清单与实际文件不匹配。

## 7. 最低验收标准

一个版本只有同时满足以下条件才算发布完成：

- 全部规定测试通过；
- Git 提交和标签可追溯；
- Gitea 发布版可下载；
- Ubuntu 上的 ZIP 哈希与本地一致；
- `latest.json` 内容正确；
- 员工网络能够访问清单和 ZIP；
- 客户端能完成版本比较、下载、校验、替换和重启；
- 失败时旧版本仍可继续使用。

这套原则与实现语言无关，可以直接作为后续桌面发布工具的业务需求和验收依据。
