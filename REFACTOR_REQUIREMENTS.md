# DocSwift 重构需求说明书

## 1. 重构目标

DocSwift 用于把多张 `.docx` 工艺卡识别、人工校对后，统一追加到一个工单系统 Excel 导入文件。

本次重构的重点不是单纯更换界面，而是把处理过程改为可以暂停、恢复、校对、追踪状态并防止重复导出的任务工作台。

## 2. 界面结构

主界面分为三个工作区域：

1. 左侧工艺卡列表
   - 显示文件名、工艺路线信息、最终工序数量和处理状态。
   - 支持添加多个 `.docx`、递归添加文件夹、移除和拖动排序。
2. 右上 Word 离线预览
   - 将 Word 在本地生成结构化 PDF 缓存。
   - 支持连续滚动、上一页、下一页、适合宽度、放大和缩小。
   - 点击最终工序时跳转到对应来源页。
3. 右下识别与校对
   - 只显示合并后的最终工序。
   - 可修改工艺路线编号、名称、工序号、工序名称和多行工序内容。
   - 支持新增、删除、合并、拆分、上移、下移和恢复识别结果。

## 3. 任务模型

一个导入任务包含：

- 多张 Word 工艺卡；
- 一个 Excel 模板；
- 一个目标 Excel；
- 自动识别开关；
- 工艺卡顺序、识别结果、人工修改、确认状态和导出记录。

关闭程序不会清理任务。程序启动后自动恢复最后打开的任务。

支持：

- 新建任务：当前任务自动归档；
- 历史任务：重新打开以前的任务；
- 清空当前任务：只清除软件中的任务数据，不删除原始 Word 和生成的 Excel。

任务数据保存在当前 Windows 用户的本地应用数据目录：

```text
%LOCALAPPDATA%\DocSwift\docswift.db
```

## 4. 工艺卡状态

状态流转：

```text
未识别 → 识别中 → 待确认 → 已确认 → 已导出
                      ↘ 识别异常
                      ↘ 源文件已变化
```

- `未识别`：刚添加，尚未执行识别。
- `识别中`：后台正在读取 Word。
- `待确认`：已识别或已修改，需要人工确认。
- `已确认`：允许进入批量导出。
- `已导出`：已成功写入当前目标 Excel。
- `识别异常`：Word 结构不符合识别要求。
- `源文件已变化`：源 Word 的大小或修改时间发生变化。

已确认内容再次编辑时，自动退回待确认。

已导出内容默认锁定。点击“重新编辑”后退回待确认；再次确认并导出时，替换目标 Excel 中该工艺路线的旧数据。

## 5. 识别规则

- 第一版只支持 `.docx`。
- 自动识别为开关，默认关闭。
- 自动识别关闭时，可识别所选工艺卡或全部未识别工艺卡。
- 识别零件图号、零件名称、工种、工序号和工序内容。
- 工种为空的连续行合并到上一条有名称的工种。
- 合并后的工序号取第一条原始工序号。
- 原始范围单独保留供校对，例如 `4～19`。
- 每一个原始步骤在最终内容中单独换行。
- 同名但不连续的工种不能合并。

## 6. 排除规则

- `待焊`是不可删除的内置排除项。
- 用户可以添加和删除自定义排除项。
- 工序名称或工序内容只要包含排除文字，该原始工序就不进入最终工序表。
- 被排除数量显示在校对区域。
- 新增排除项后，需要重新识别现有工艺卡。
- 排除后不重新编号后续工序。

## 7. 自动保存

以下操作立即写入 SQLite：

- 添加和移除工艺卡；
- 拖动排序；
- 模板和目标 Excel 路径；
- 自动识别开关；
- 工艺路线编号和名称；
- 工序字段修改；
- 新增、删除、合并、拆分和移动；
- 确认、重新编辑和导出状态；
- 自定义排除项。

## 8. 源文件变化

程序每五秒检查一次源 `.docx`：

- 检测到变化后保留旧的人工校对结果；
- 状态改为“源文件已变化”；
- 暂时禁止确认和导出；
- 用户可以重新识别，或选择继续使用旧结果；
- 选择继续使用后仍需再次人工确认。

## 9. Excel 导出

- 多张工艺卡统一写入一个目标 Excel。
- 只导出“已确认且未导出”的工艺卡。
- 待确认、识别异常和源文件已变化的工艺卡禁止导出。
- 已导出的工艺卡自动跳过。
- 默认按左侧列表顺序写入，支持拖动调整顺序。
- 工艺路线编号在同一个目标 Excel 中唯一。
- 通过表头文字识别 Excel 列，不依赖固定列顺序。
- 固定从第 2 行开始写入。
- 所有输出内容水平、垂直居中并自动换行。
- 所有输出单元格添加黑色细框线。
- 合并后的多步骤内容自动调整行高。

批量导出采用完整事务：

1. 复制现有 Excel 或加载模板到临时文件；
2. 所有工艺卡写入临时文件；
3. 如任意步骤失败，删除临时文件，原 Excel 保持不变；
4. 全部成功后一次性替换目标 Excel；
5. 最后将工艺卡标记为已导出。

同一工艺卡重新导出到同一个 Excel 时，通过唯一工艺路线编号删除旧行后写入新行，避免重复数据。

## 10. 本地预览说明

工艺文件必须完全离线处理，不调用 WPS 在线转换服务。

当前采用本地结构化预览：

- 从 `.docx` 的段落和表格生成 PDF；
- 缓存保存在 `%LOCALAPPDATA%\DocSwift\preview-cache`；
- 不修改原始 Word；
- 不上传任何文件；
- 来源页定位以软件生成的离线预览页码为准。

由于不调用 WPS 私有转换接口，预览分页可能与 WPS 打印分页存在细微差异，但识别和 Excel 输出仍以 Word 表格原始内容为准。
