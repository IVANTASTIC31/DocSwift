# DocSwift 项目总结与功能演进

本文档从项目恢复和多轮实际工艺卡验证中提炼需求、设计决策、问题原因与修改记录，便于后续维护和交接。示例仅描述字段和工序关系，不包含客户工艺文件。

## 1. 项目目标

DocSwift 是一个 Windows 本地桌面工具，用于把 Word 工艺卡转换为工单系统可导入的 Excel 工艺路线文件。

核心流程：

1. 选择一个或多个 Word 工艺卡。
2. 选择 Excel 导入模板。
3. 解析并预览零件信息和工序。
4. 根据业务规则合并连续工序、过滤禁止工序。
5. 继承模板样式并生成标准九列 Excel 文件。

## 2. 技术结构

- `app.py`：Tkinter 图形界面、命令行参数、后台线程、预览和日志。
- `core.py`：Word 表格解析、工序分组、业务过滤、Excel 列映射和样式写入。
- `run.bat`：Windows 启动入口，优先使用项目虚拟环境，然后尝试系统 Python。
- `requirements.txt`：`python-docx` 和 `openpyxl` 依赖。
- `README.md`：安装、使用和规则说明。

## 3. 数据识别规则

### 3.1 Word 工艺卡

程序从 Word 表格中识别：

- 零件图号
- 零件名称
- 工种
- 工序号
- 工序内容

工序号必须是数字，支持带小数点或连字符的扩展形式。缺少零件图号、零件名称或有效工序时，程序会停止并给出错误。

### 3.2 Excel 标准列

标准输出为九列：

1. 工艺路线编号
2. 工艺路线名称
3. 工序/工艺路线列表
4. 工序号
5. 工序内容
6. 类型
7. 报工数配比
8. 是否锁定为最后一道工序
9. 工时/分钟

程序优先按表头文字识别列。标准模板 D1 为空时，也会按 `D=工序号、E=工序内容` 补全，并把 D1 写成“工序号”。

## 4. 功能演进

### 阶段一：恢复项目并确认基础能力

- 确认项目为 Python/Tkinter 桌面程序。
- 验证 `python-docx` 和 `openpyxl` 依赖。
- 梳理 Word 解析、Excel 模板写入和追加模式。
- 确认源文件采用 UTF-8，早期看到的中文乱码来自 PowerShell 读取编码，不是源码损坏。

### 阶段二：连续工序合并与换行

业务要求同一工种的连续步骤只占一条 Excel 记录。例如“车”包含工序 4～19时：

- 工种写“车”。
- 工序号保留起始号 `4`。
- 工序 4～19 的内容在同一单元格中逐条换行。
- 下一道工序继续使用工艺卡原号，例如 `20`，不按 Excel 行号重新编号。

程序根据工艺卡的表示方式处理：有名称的工种之后，工种单元格为空的连续行视为该工种的后续步骤。

### 阶段三：样式和导入格式完善

- 合并后的多步骤行自动增加行高。
- 所有表头和输出数据水平、垂直居中，并启用自动换行。
- A～I 的每个输出单元格统一添加黑色细框线，包括空白列。
- D 列表头统一命名为“工序号”。
- 标准九列模板即使 D1 为空，也能正确写入工序号。

### 阶段四：默认生成行为调整

- 默认起始行由第 6 行改为第 2 行。
- “追加到已有输出文件”默认关闭。
- 默认从模板重新生成，并清理起始行之后的旧明细。
- 如确有需要，可以在界面勾选追加，或在命令行使用 `--append`。
- 程序关闭后不保留本次选择、预览或日志数据。

### 阶段五：工单系统强制过滤

“待焊”会影响工单系统数据，因此设为不可配置的强制规则：

- 工种或工序内容只要包含“待焊”，预览和输出都会删除该工序。
- “待焊。”等带标点形式同样删除。
- 删除后不重排其他工序号。

### 阶段六：修复单条续行未合并

实际工艺卡暴露出一个边界问题：

- 旧实现只有在一个工种后存在至少两条空白工种续行时才合并。
- 因此“钻 14 + 空白工种 15”或“钻 12 + 空白工种 13”不会合并。
- 同一个文件中，拥有多条续行的“钻 24”却可以正常合并，表现不一致。

修复后，只要存在一条或多条空白工种续行就会合并。实际文件回归结果包括：

- `钻 14（两步内容）→ 检验 16`
- `钻 12（两步内容）→ 检验 14`
- `钻 24（多步内容）`继续正常合并

### 阶段七：启动与分发改进

原 `run.bat` 包含开发电脑的 Python 绝对路径。复制到其他电脑后，如果对方没有安装 Python，窗口会一闪而过。

发布版启动脚本改为：

1. 优先使用项目 `.venv`。
2. 再尝试 Windows Python Launcher `py -3`。
3. 最后尝试 PATH 中的 `python`。
4. 未安装 Python、缺少依赖或程序异常时显示明确提示并暂停窗口。

源代码分发仍要求目标电脑安装 Python 和依赖；如需完全免安装分发，可在后续版本增加 PyInstaller 打包流程。

## 5. 已确认的业务约束

- 工序号来自工艺卡，不得按输出行数重新编号。
- 空白工种续行归入上一条有名称的工种。
- 每个连续步骤在内容单元格中独立换行。
- “待焊”必须从预览和输出中排除。
- 默认覆盖生成，避免旧数据重复导入。
- 输出必须具备完整框线、居中和可读行高。

## 6. 验证方式

历次修改采用以下检查：

- `python -m py_compile app.py core.py` 语法检查。
- 使用真实 Word 工艺卡检查解析后的原始工序和合并预览。
- 使用真实 Excel 模板生成回归文件。
- 核对表头、原始工序号、换行数量、行数、边框和对齐方式。
- 扫描输出中是否残留“待焊”或 Excel 公式错误。
- 渲染关键 Excel 区域进行视觉检查。

## 7. 后续可选改进

- 使用 PyInstaller 构建无需 Python 的 Windows 可执行程序。
- 增加自动化单元测试，覆盖单条续行、多条续行、“待焊”和空白 D1 模板。
- 把强制过滤词集中到配置或规则模块；若业务要求“待焊”永远不可配置，则保留代码级常量。
- 增加版本号、打包脚本和 GitHub Releases 发布流程。
