# DocSwift 项目总结与功能演进

本文档从项目恢复和多轮实际工艺卡验证中提炼需求、设计决策、问题原因与修改记录，便于后续维护和交接。示例仅描述字段和工序关系，不包含客户工艺文件。

## 1. 项目目标

DocSwift 是一个 Windows 本地桌面工具，用于把 Word 工艺卡转换为工单系统可导入的 Excel 工艺路线文件。

核心流程：

1. 选择一个或多个 Word 工艺卡。
2. 选择 Excel 导入模板。
3. 解析并预览零件信息和工序。
4. 根据业务规则合并连续工序、过滤禁止工序。
5. 继承模板样式并生成标准九列 Excel 文件。

## 2. 技术结构

- `app.py`：PySide6 主窗口、状态列表、PDF预览、工序校对和后台任务。
- `domain.py`：任务、工艺卡、最终工序、排除规则和导出记录领域模型。
- `project_store.py`：SQLite 自动保存、历史任务和状态流转。
- `services.py`：DOCX识别与批量原子导出编排。
- `preview_service.py`：完全离线的 DOCX结构化 PDF预览和来源页定位。
- `core.py`：Word 表格解析、工序分组、业务过滤、Excel 列映射和样式写入。
- `run.bat`、`DocSwift.vbs`：Windows 无终端启动入口。
- `requirements.txt`：`python-docx`、`openpyxl`、`PySide6`和`pypdf`依赖。
- `REFACTOR_REQUIREMENTS.md`：重构后的正式业务规则。

## 3. 数据识别规则

### 3.1 Word 工艺卡

程序从 Word 表格中识别：

- 零件图号
- 零件名称
- 工种
- 工序号
- 工序内容

工序号必须是数字，支持带小数点或连字符的扩展形式。缺少零件图号、零件名称或有效工序时，程序会停止并给出错误。

### 3.2 Excel 标准列

标准输出为九列：

1. 工艺路线编号
2. 工艺路线名称
3. 工序/工艺路线列表
4. 工序号
5. 工序内容
6. 类型
7. 报工数配比
8. 是否锁定为最后一道工序
9. 工时/分钟

程序优先按表头文字识别列。标准模板 D1 为空时，也会按 `D=工序号、E=工序内容` 补全，并把 D1 写成“工序号”。

## 4. 功能演进

### 阶段一：恢复项目并确认基础能力

- 确认项目为 Python/Tkinter 桌面程序。
- 验证 `python-docx` 和 `openpyxl` 依赖。
- 梳理 Word 解析、Excel 模板写入和追加模式。
- 确认源文件采用 UTF-8，早期看到的中文乱码来自 PowerShell 读取编码，不是源码损坏。

### 阶段二：连续工序合并与换行

业务要求同一工种的连续步骤只占一条 Excel 记录。例如“车”包含工序 4～19时：

- 工种写“车”。
- 工序号保留起始号 `4`。
- 工序 4～19 的内容在同一单元格中逐条换行。
- 下一道工序继续使用工艺卡原号，例如 `20`，不按 Excel 行号重新编号。

程序根据工艺卡的表示方式处理：有名称的工种之后，工种单元格为空的连续行视为该工种的后续步骤。

### 阶段三：样式和导入格式完善

- 合并后的多步骤行自动增加行高。
- 所有表头和输出数据水平、垂直居中，并启用自动换行。
- A～I 的每个输出单元格统一添加黑色细框线，包括空白列。
- D 列表头统一命名为“工序号”。
- 标准九列模板即使 D1 为空，也能正确写入工序号。

### 阶段四：默认生成行为调整

- 默认起始行由第 6 行改为第 2 行。
- “追加到已有输出文件”默认关闭。
- 默认从模板重新生成，并清理起始行之后的旧明细。
- 如确有需要，可以在界面勾选追加，或在命令行使用 `--append`。
- 程序关闭后不保留本次选择、预览或日志数据。

### 阶段五：工单系统强制过滤

“待焊”会影响工单系统数据，因此设为不可配置的强制规则：

- 工种或工序内容只要包含“待焊”，预览和输出都会删除该工序。
- “待焊。”等带标点形式同样删除。
- 删除后不重排其他工序号。

### 阶段六：修复单条续行未合并

实际工艺卡暴露出一个边界问题：

- 旧实现只有在一个工种后存在至少两条空白工种续行时才合并。
- 因此“钻 14 + 空白工种 15”或“钻 12 + 空白工种 13”不会合并。
- 同一个文件中，拥有多条续行的“钻 24”却可以正常合并，表现不一致。

修复后，只要存在一条或多条空白工种续行就会合并。实际文件回归结果包括：

- `钻 14（两步内容）→ 检验 16`
- `钻 12（两步内容）→ 检验 14`
- `钻 24（多步内容）`继续正常合并

### 阶段七：启动与分发改进

原 `run.bat` 包含开发电脑的 Python 绝对路径。复制到其他电脑后，如果对方没有安装 Python，窗口会一闪而过。

发布版启动脚本改为：

1. 优先使用项目 `.venv`。
2. 再尝试 Windows Python Launcher `py -3`。
3. 最后尝试 PATH 中的 `python`。
4. 未安装 Python、缺少依赖或程序异常时显示明确提示并暂停窗口。

源代码分发仍要求目标电脑安装 Python 和依赖；如需完全免安装分发，可在后续版本增加 PyInstaller 打包流程。

### 阶段八：工艺卡使用状态提示

Windows 原生文件选择窗口不允许 Tkinter修改单个文件的颜色，因此改为程序内置工艺卡选择窗口：

- 成功生成过 Excel 的工艺卡显示为灰色并标注“已使用”。
- 使用记录保存在当前 Windows 用户的本地应用数据目录，跨程序重启有效。
- 记录同时校验文件大小和修改时间；工艺卡更新后自动恢复为“未使用”。
- 支持“全选未使用”、刷新、切换文件夹和清除使用记录。
- 只有成功生成 Excel 才标记，避免误选或解析失败导致错误记录。

### 阶段九：修复 Windows 批处理启动

加入中文错误提示后，`run.bat` 曾以 UTF-8/Unix LF 保存。部分 Windows `cmd.exe` 会错误解析多字节文本并截断后续命令，表现为 `hon.exe`、`enpyxl` 等残片“不是内部或外部命令”。

修复内容：

- 启动脚本改为纯 ASCII 文本和 Windows CRLF 换行。
- Python 命令与参数分开保存，避免带参数的命令字符串解析异常。
- 除 `.venv`、`py -3` 和系统 `python` 外，增加基于 `%USERPROFILE%` 的 Codex Python 回退，不写死具体用户名。
- 失败提示保留窗口，便于用户看到真实错误。

### 阶段十：隐藏启动终端

- 新增 `DocSwift.vbs`，通过 Windows Script Host 在隐藏窗口中执行启动脚本。
- `run.bat` 自动查找与当前 Python 配套的 `pythonw.exe`，界面启动后不再保留命令行窗口，也不依赖 VBS 功能是否启用。
- 双击 `DocSwift.vbs` 完全不显示终端；双击 `run.bat` 仅可能在启动瞬间短暂闪一下。
- 启动失败时弹出提示，并保留 `DocSwift-startup.log` 供排查。
- 命令行执行 `run.bat --console` 可继续使用可见终端调试。

### 阶段十一：PySide6 工作台重构

原 Tkinter 界面适合一次性选择、预览和导出，但无法很好支持多工艺卡状态、Word 分页预览、复杂人工校对、自动保存和历史任务恢复。

本阶段完成：

- 图形界面迁移到 PySide6。
- 左侧改为工艺卡状态列表，取消单独的灰色“已使用”历史。
- 右上增加完全离线的 Word 结构化 PDF 预览、分页、缩放和来源页跳转。
- 右下增加合并后最终工序的可编辑表格。
- 支持工序增删、合并、拆分、移动、恢复识别、确认和重新编辑。
- 新增 SQLite 任务数据层，自动保存全部人工修改。
- 支持新建任务、历史任务和启动时恢复最后任务。
- 自动识别改为默认关闭的任务开关。
- `待焊`保留为不可删除规则，并支持用户添加自定义排除项。
- 每五秒检测源 `.docx` 是否发生变化，保留旧校对结果并阻止误导出。
- 批量导出只处理已确认且未导出的工艺卡。
- Excel 写入改为临时文件完成后再替换的原子操作。
- 工艺路线编号在目标 Excel 中唯一，重新导出时替换旧行，避免重复数据。
- 新增数据层、识别服务、离线预览、导出服务和 UI 冒烟测试。
- 增加单实例锁，防止重复双击造成两个窗口同时修改任务数据库。

### 阶段十二：固定浅色主题与安全更新入口

Windows 使用深色系统主题时，Qt 的系统调色板曾把按钮文字设为浅色，而界面样式又把按钮背景固定为白色，导致工具栏和按钮看起来没有文字。

本阶段完成：

- 程序启动时强制使用 Fusion 浅色调色板。
- 明确设置普通、只读、禁用、菜单、表格、复选框和提示框的前景色。
- 顶部增加“检查更新”，显示当前版本并在后台检查 GitHub 正式 Release。
- 更新包采用固定命名，下载后进行 SHA-256 完整性校验。
- 源码版和免安装版下载完成后打开压缩包，由用户解压到新目录，避免运行中覆盖程序导致损坏。
- 增加 PyInstaller Windows 免安装包构建脚本和校验文件生成流程。

### 阶段十三：Word 原版表格预览

原预览器把 DOCX 表格转换成 Qt HTML 后再打印为 PDF。复杂工艺卡包含 20 列网格、横向与纵向合并单元格和固定分页，HTML 重排会造成边框丢失、列宽失真并把三页内容扩展成八页。

本阶段完成：

- 优先调用本机 WPS 的 `kwps.Application` 离线导出 PDF。
- WPS 不可用时尝试 Microsoft Word，再降级为内置 HTML 渲染。
- HTML 降级模式补充显式表格边框，避免完全看不出单元格。
- 预览缓存版本升级，旧的错误预览会自动失效并重新生成。
- 来源页定位改为搜索实际预览 PDF。
- 使用“工种 + 工序号 + 工序内容”作为定位锚点，解决多个“检验”全部跳到第一页的问题。
- 合并工序继续支持跨页范围，例如工序 `26～30` 定位为第 `2～3` 页。

### 阶段十四：公司内网代码仓库与更新服务

为避免同事受 GitHub、Gitee 外网连接和 Python 依赖安装影响，在公司
Ubuntu 虚拟机上建立独立的代码与软件分发服务。

本阶段完成：

- 使用 Docker 部署公司内部 Gitea，代码仓库地址为
  `http://192.168.100.3:3000/IVANTASTIC31/DocSwift`。
- 本地 Git 增加 `company` 远程地址，与 GitHub、Gitee 三个仓库并行保留。
- 内部仓库使用私有可见性，并同步完整提交历史和正式版本标签。
- 使用 Nginx 建立 `/updates/docswift/` 软件分发目录。
- `latest.json` 记录版本号、发布时间、下载地址、文件大小和 SHA-256。
- Windows 免安装包按版本保存在 `releases/v版本号/`，不同版本互不覆盖。
- 客户端更新检查改为优先访问公司服务器；公司服务器不可用时才回退
  GitHub，避免公网波动影响日常更新。
- 公司服务器明确返回当前版本时，不再越过内部发布策略查询公网版本。
- 内部下载地址必须与清单位于同一受信任目录，文件名必须与版本号一致。
- 更新包下载后继续执行字节数和 SHA-256 双重校验，校验失败立即删除。
- 使用真实内网地址完成清单读取、54,441,057 字节下载和哈希联调。
- 客户端版本升级到 `v0.2.1`，作为首次支持公司内网更新的过渡版本。

## 5. 已确认的业务约束

- 工序号来自工艺卡，不得按输出行数重新编号。
- 空白工种续行归入上一条有名称的工种。
- 每个连续步骤在内容单元格中独立换行。
- “待焊”必须从预览和输出中排除。
- 默认覆盖生成，避免旧数据重复导入。
- 输出必须具备完整框线、居中和可读行高。

## 6. 验证方式

历次修改采用以下检查：

- `python -m py_compile app.py core.py` 语法检查。
- 使用真实 Word 工艺卡检查解析后的原始工序和合并预览。
- 使用真实 Excel 模板生成回归文件。
- 核对表头、原始工序号、换行数量、行数、边框和对齐方式。
- 扫描输出中是否残留“待焊”或 Excel 公式错误。
- 渲染关键 Excel 区域进行视觉检查。

## 7. 后续可选改进

- 为公司更新服务配置内部域名和 HTTPS，替换当前固定内网 IP。
- 为 Gitea 和 `/srv/docswift-updates` 制定定期备份与恢复演练。
- 将版本清单和上传操作封装为发布脚本，减少人工输入版本号和校验值。
- 评估在已充分验证后加入自动解压和原地升级；当前继续采用下载、校验、
  打开压缩包和人工切换目录的稳妥流程。
